includes:
    - phpstan-baseline.neon

parameters:
    level: 6
    paths:
        - app/

    # View templates contain helper functions defined at runtime
    excludePaths:
        - app/Modules/*/Views/**
